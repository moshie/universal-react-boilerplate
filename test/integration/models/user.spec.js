// Vendor
import { expect } from 'chai'

describe.skip('User', () => {

    it('should be invalid if email is empty')

    it('should be invalid if first_name is empty')

    it('should be invalid if password is empty')

    it('should return fullname')

    it('should hash the password when saved')

    it('should add timestamps')

})