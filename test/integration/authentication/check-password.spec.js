// Vendor
import { expect } from 'chai'

// Helpers
import fixtures from '../../fixtures/model-users'

// Model
import User from '../../../src/api/authentication/models/User'

// Testing
import checkPassword from '../../../src/api/authentication/check-password'

describe('#checkPassword()', () => {

    it('should return a promise')

    it('should reject with error if a user was not provided')

    it('should reject if the password provided is empty or invalid')

    it('should reject in the result an unexpected error occurs')

    it('should reject with error if authentication failed')

    it('should resolve with the user if authentication passed')

})